version: '3.8'

services:
  workshop-getting-started-website:
    image: ${REGISTRY:-maddev77}/workshop-getting-started:${PLATFORM:-linux}-${TAG:-latest}
    build:
      context: .
      dockerfile: WebSite/Dockerfile
